#include <iostream>
#include <limits.h>
using namespace std;
int max_sum_subarray(int a){
}
int main(){
    int a[] = {3,-2,5,-1};
    int n = sizeof(a)/sizeof(a[0]);
    int mss = max_sum_subarray(a,n);
    cout<<mss<<endl;
}
